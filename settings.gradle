rootProject.name = 'marmot.sample'

def githubDir = file('../../../github')
['utils'].each {
	include ":$it"
	project(":$it").projectDir = file("$githubDir/$it")
}

['marmot.api'].each {
	include ":$it"
	project(":$it").projectDir = file("../../$it")
}